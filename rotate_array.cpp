#include <iostream>
using namespace std;

int main(){

int arr[]={1,2,3,4,5};
int len=sizeof(arr)/sizeof(arr[0]);

    // for(auto i : arr){
    for( int i = 0; i <= len; i++ ){
    if( arr[i] = i-1 ){
        arr[0] = 5;

        cout << arr[i];
    }
}
// int newarr[]=arr.pop(5);
  

//   cout<<newarr.push(newarr);;

}